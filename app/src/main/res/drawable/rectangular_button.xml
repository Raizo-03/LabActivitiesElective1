<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/black" /> <!-- Button color -->
    <stroke
        android:width="2dp"
        android:color="@color/black" /> <!-- Optional border -->
    <corners android:radius="0dp" /> <!-- Rectangular edges -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" /> <!-- Optional padding -->
</shape>
